<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuecheng.content.mapper.TeachplanMapper">
    <select id="QueryTeachPlanById" resultType="com.xuecheng.content.model.dto.TeachplanDto" parameterType="Long">
            with recursive t1 as(
                select * from teachplan where course_id=#{id} and parentid=0
                union all
                select t2.*
                from teachplan t2 inner join t1 on t2.parentid=t1.id
            )
            select * from t1 order by parentid,orderby
    </select>

</mapper>
